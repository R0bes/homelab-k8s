apiVersion: v1
kind: Service
metadata:
  name: authentik-outpost
  namespace: fourierfun
spec:
  type: ExternalName
  externalName: authentik-server.authentik.svc.cluster.local
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP
      name: http
