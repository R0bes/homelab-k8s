# Example only: copy, set token, apply manually (do not commit real tokens).
# Requires namespace homepage (Argo CreateNamespace or kubectl create ns homepage).
apiVersion: v1
kind: Secret
metadata:
  name: homepage-content-git
  namespace: homepage
type: Opaque
stringData:
  token: REPLACE_WITH_GITHUB_PAT_READ_HOMEPAGE_CONTENT
